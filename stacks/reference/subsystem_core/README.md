# Subsystem Core Reference Stack

Wires the Phase 1 autonomous subsystem foundation:

- Event hub for subsystem events.
- Customer BFF service with HTTP API, Lambda functions, DynamoDB, and event publication.
- A secondary provider alias to demonstrate future multi-region support.

This stack is intended as a reference composition, not as a shared child module for every team.
